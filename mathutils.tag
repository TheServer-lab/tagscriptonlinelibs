<module>
  <doit name="sqrt" args="x">
    <giveback value="__import__('math').sqrt(float(x))"/>
  </doit>
  <doit name="pi">
    <giveback value="__import__('math').pi"/>
  </doit>
</module>

